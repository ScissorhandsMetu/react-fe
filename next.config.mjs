/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["example.com", "via.placeholder.com"], // Replace with the domains in your dummy data
  },
};

export default nextConfig;
